import {render, screen} from "@testing-library/react";
import {RepositorySearch} from "@/template/RepositorySearchTemplate/RepositorySearchTemplate";

describe("SearchPage", () => {
  it.todo("検索結果を表示する", async () => {});
});
